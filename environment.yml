name: luna_env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy>=1.21
  - matplotlib>=3.5
  - pandas>=1.3
  - scipy>=1.7
  - rasterio>=1.3
  - gdal>=3.4         # Includes `osgeo` bindings
  - pip
  - pip:
      - tqdm
      - geopandas>=0.10
      - shapely>=1.7  